package com.example.planyourtrip.model;

/**
 * Phase 7.29 — whether a rate plan prices itself directly ({@code BASE}) or is
 * derived from a parent plan by an adjustment ({@code DERIVED}).
 */
public enum RateSourceType {
    BASE, DERIVED
}
